{
  "name": "cardiopredict-backend",
  "version": "1.0.0",
  "description": "Proxy Node.js que reenvía peticiones al endpoint de Databricks ocultando el bearer token y resolviendo CORS.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
